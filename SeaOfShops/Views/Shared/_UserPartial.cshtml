

<div class=" navbar-collapse " style="position: absolute; right: 200px">
    <div class="nav-item navbar-nav">
            @await Component.InvokeAsync("Cart")
    </div>
    <ul class="navbar-nav">
        <li class="nav-item dropdown">
            <a class="nav-link
            dropdown-toggle"
               id="navbarDropdown2"
               role="button"
               data-bs-toggle="dropdown"
               aria-haspopup="true"
               aria-expanded="false">
                @await Component.InvokeAsync("Avatar", User.Identity.Name)
            </a>
            <ul class="dropdown-menu"
                aria-labelledby="navbarDropdown2">
                <li>
                    <a href="#" class="dropdown-item">
                        @await Component.InvokeAsync("Avatar", User.Identity.Name)
                        @User.Identity.Name
                    </a>
                </li>
                <li class="dropdown-divider"
                    aria-labelledby="navbarDropdown2">
                </li>
                <li>
                    <a asp-controller="Product" asp-action="UserProducts"
                       class="dropdown-item">Мои товары</a>
                </li>
                <li>
                    <form asp-controller="Account" asp-action="Logout">
                       <button class="dropdown-item" type="submit">Log Off</button>
                    </form>
                </li>
            </ul>
        </li>
    </ul>
</div>